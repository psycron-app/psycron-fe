# Psycron Frontend Environment Configuration
# Copy this file to .env and fill in your values
# NEVER commit .env files with real values to version control

# =============================================================================
# API Configuration
# =============================================================================

# Backend API URL
# Development: http://localhost:8080/api/v1
# Production: https://api.psycron.app/api/v1
VITE_PSYCRON_BASE_API_URL=http://localhost:8080/api/v1

# =============================================================================
# Third-Party Services (Optional)
# =============================================================================

# IP Geolocation API Key (ipgeolocation.io)
# Used for: Automatic timezone detection
# Get your key at: https://ipgeolocation.io/
VITE_IP_GEO_KEY=your_ipgeo_api_key_here

# Google Maps API Key
# Used for: Address autocomplete, maps display
# Get your key at: https://console.cloud.google.com/apis/credentials
# Required APIs: Maps JavaScript API, Places API
VITE_GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here

# Google Analytics Measurement ID
# Used for: Usage analytics and tracking
# Get your ID at: https://analytics.google.com/
# Format: G-XXXXXXXXXX
VITE_GA_MEASUREMENT_ID=G-XXXXXXXXXX


VITE_ENABLE_SUBSCRIPTION_MOCK=boolean

# =============================================================================
# IMPORTANT SECURITY NOTES
# =============================================================================
#
# 1. NEVER commit this file with real values
# 2. NEVER include backend secrets (JWT_SECRET, DB passwords) in frontend
# 3. All VITE_ prefixed variables are exposed to the browser
# 4. Use environment-specific .env files:
#    - .env.development (local development)
#    - .env.production (production builds)
#    - .env.staging (staging environment)
#
# For CI/CD, set these as environment variables in your deployment platform
# (Vercel, Netlify, GitHub Actions, etc.)
#
